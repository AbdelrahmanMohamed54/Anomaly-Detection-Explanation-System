# Project: AI Anomaly Detection & Explanation System

## What this project does
A hybrid AI system combining a TensorFlow time-series anomaly detection model
with a LangChain LLM agent that automatically generates plain-language root
cause analysis (RCA) reports when production anomalies are flagged.
Reports delivered in English and German via a Grafana-integrated dashboard.

## Architecture
Sensor Data (time-series CSV / PostgreSQL)
    → data_preprocessing.py (feature engineering, normalization)
    → TensorFlow anomaly_detector.py (LSTM/Autoencoder — flags anomalies)
    → LangChain RCA Agent (rca_agent.py — root cause analysis)
        → Tool 1: historical_query.py (PostgreSQL — past similar anomalies)
        → Tool 2: semantic_search.py (ChromaDB — incident report retrieval)
        → Tool 3: action_recommender.py (ChromaDB — maintenance log RAG)
    → report_generator.py (bilingual EN/DE structured report)
    → FastAPI backend (api/main.py)
    → Grafana dashboard (real-time alerts + LLM report panel)

## Tech stack
- Anomaly detection: TensorFlow (LSTM autoencoder on time-series data)
- LLM agent: LangChain + Google Gemini 2.5 Flash (primary)
- LLM fallback: Groq Llama 3.3 70B
- Historical DB: PostgreSQL via SQLAlchemy + psycopg2
- Semantic search: ChromaDB + sentence-transformers (all-MiniLM-L6-v2)
- API: FastAPI
- Dashboard: Grafana (importable dashboard.json)
- Containers: Docker + Docker Compose

## File structure
- model/data_preprocessing.py — normalization, windowing, feature engineering
- model/train.py — LSTM autoencoder training pipeline
- model/anomaly_detector.py — inference wrapper, threshold logic, anomaly scoring
- agent/rca_agent.py — LangChain agent with 3 tools, generates RCA
- agent/report_generator.py — formats RCA into bilingual EN/DE report
- agent/tools/historical_query.py — PostgreSQL tool for past anomaly lookup
- agent/tools/semantic_search.py — ChromaDB tool for incident report retrieval
- agent/tools/action_recommender.py — ChromaDB tool for maintenance log RAG
- api/main.py — FastAPI backend, /analyze endpoint, /health endpoint
- scripts/generate_synthetic_data.py — generates realistic sensor CSV data
- scripts/seed_postgres.py — seeds PostgreSQL with historical anomaly records
- scripts/ingest_incident_reports.py — embeds reports into ChromaDB
- scripts/ingest_maintenance_logs.py — embeds maintenance logs into ChromaDB
- grafana/dashboard.json — importable Grafana dashboard config

## Pydantic models (define in api/main.py)
- SensorReading: sensor_id, timestamp, values: dict[str, float]
- AnomalyEvent: sensor_id, anomaly_type, severity: float, timestamp, detected_values
- HistoricalAnomaly: id, sensor_id, anomaly_type, timestamp, duration, resolution
- RCAReport: sensor_id, severity, anomaly_summary, root_cause, similar_incidents,
             recommended_actions, escalate: bool, sources: list[str],
             generation_time_seconds: float, language: str

## TensorFlow model design
- Architecture: LSTM Autoencoder (encoder-decoder)
- Input: sliding window of 50 time steps, multivariate sensor readings
- Output: reconstruction error per time step
- Anomaly threshold: mean + 3*std of reconstruction error on normal training data
- Training data: synthetic normal sensor data generated by generate_synthetic_data.py
- Save/load: model/saved_models/ directory using model.save() / tf.keras.models.load_model()

## Key design decisions
- Synthetic data first: generate_synthetic_data.py creates realistic sensor patterns
  so the model can be trained without real proprietary data
- Three independent ChromaDB collections: incident_reports, maintenance_logs (separate)
- PostgreSQL for structured historical queries, ChromaDB for semantic search
- Bilingual output: all reports generated in both EN and DE in one API call
- Grafana reads from FastAPI via JSON data source plugin (no direct DB connection)
- Provider-agnostic LLM: swap Gemini for Groq by changing one environment variable

## Environment variables
GOOGLE_API_KEY — primary LLM (Gemini 2.5 Flash)
GROQ_API_KEY — fallback LLM (Groq Llama 3.3 70B)
POSTGRES_URL — PostgreSQL connection string
LANGFUSE_PUBLIC_KEY — observability (optional for this project)
LANGFUSE_SECRET_KEY — observability (optional for this project)

## Commands
- Generate synthetic data: python scripts/generate_synthetic_data.py
- Seed PostgreSQL: python scripts/seed_postgres.py
- Ingest incident reports: python scripts/ingest_incident_reports.py
- Ingest maintenance logs: python scripts/ingest_maintenance_logs.py
- Train model: python model/train.py
- Start API: uvicorn api.main:app --reload --port 8000
- Run tests: pytest tests/
- Full Docker stack: docker-compose up --build

## Coding conventions
- All functions must have type hints
- All modules must have docstrings
- Use async/await for all LLM and API calls
- Pydantic v2 models for all API inputs/outputs
- Never hardcode API keys — always use os.getenv()
- Log anomaly detections and RCA generations with timestamps

## Git workflow — mandatory after every change
After every meaningful change run:
    git add .
    git commit -m "type: description"
    git push origin main

Commit types: feat / fix / refactor / test / docs / chore

## Build order — follow this exactly
### Session 1 — COMPLETE
1. scripts/generate_synthetic_data.py — create training + test sensor data ✓
2. model/data_preprocessing.py — windowing, normalization, feature engineering ✓
3. model/train.py — train the LSTM autoencoder on synthetic data ✓
4. model/anomaly_detector.py — inference wrapper with threshold logic ✓

### Session 2 — COMPLETE
5. scripts/seed_postgres.py — seed historical anomaly database ✓
6. scripts/ingest_incident_reports.py + ingest_maintenance_logs.py — ChromaDB ✓
   Knowledge base: 90 incident report chunks + 102 maintenance log chunks in ChromaDB
   PostgreSQL: requires `docker-compose up db` or local postgres before running seed_postgres.py

### Session 3 — COMPLETE
7. agent/tools/historical_query.py — PostgreSQL tool ✓
8. agent/tools/semantic_search.py — ChromaDB incident report tool ✓
9. agent/tools/action_recommender.py — ChromaDB maintenance log tool ✓
10. agent/rca_agent.py — LangChain agent wiring all 3 tools ✓
    Uses LangChain 1.2.x / LangGraph create_agent API with response_format=RCAStructuredOutput
    Escalation override: severity >= 0.8 forces escalate=True regardless of LLM output
    agent/schemas.py — shared Pydantic models (RCAReport, RCAStructuredOutput, etc.)
    agent/report_generator.py — stub (interface locked; full bilingual impl in Session 4)
    29/29 tests passing across all sessions

### Session 4 — COMPLETE
11. agent/report_generator.py — bilingual EN/DE report formatter (full impl) ✓
    ReportGenerator class: generate(dict, language) + generate_bilingual(dict) -> tuple
    German translation via second LLM call with structured output (_TranslatedFields schema)
    generate_report() sync wrapper kept for rca_agent.py backward compatibility
12. api/main.py — FastAPI backend ✓
    Endpoints: GET /health, POST /analyze, POST /simulate, GET /history
    Startup lifespan loads TF model, verifies PostgreSQL + ChromaDB
    CORS middleware enabled; in-memory history (last 10 analyses)
    End-to-end verified: /simulate with real Gemini LLM produced full RCA report
    39/39 tests passing across all sessions

### Session 5 onwards
13. grafana/dashboard.json — Grafana dashboard configuration
14. docker-compose.yml + Dockerfile — containerization